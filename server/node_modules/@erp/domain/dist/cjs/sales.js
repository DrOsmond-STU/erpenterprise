"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.termDays = exports.agingKey = exports.AGING_BUCKETS = exports.daysBetween = exports.addDays = exports.RAW_MATERIALS = exports.FINISHED_GOODS = exports.COGS_ACCOUNT = exports.PPN_OUT = exports.REVENUE_SERVICE = exports.REVENUE_GOODS = void 0;
exports.salesTotals = salesTotals;
exports.lineProblems = lineProblems;
exports.invoiceStatus = invoiceStatus;
exports.overdueDays = overdueDays;
exports.aging = aging;
exports.creditCheck = creditCheck;
/**
 * Penjualan & piutang (dok. 07 §4, §10.2): perhitungan dokumen, status faktur,
 * umur piutang, dan pemeriksaan plafon kredit. Murni — dipakai API dan web.
 */
const money_js_1 = require("./money.js");
exports.REVENUE_GOODS = '4-1000';
exports.REVENUE_SERVICE = '4-2000';
exports.PPN_OUT = '2-1400';
exports.COGS_ACCOUNT = '5-1000';
exports.FINISHED_GOODS = '1-1500';
exports.RAW_MATERIALS = '1-1400';
/** Neto per baris = qty × harga × (1 − diskon%), dibulatkan ke rupiah; PPN 11% dari total neto. */
function salesTotals(lines, rate = money_js_1.PPN_RATE) {
    let subtotal = 0, netGoods = 0, netService = 0;
    const nets = lines.map((l) => {
        const gross = (0, money_js_1.round)(l.qty * l.price);
        const net = (0, money_js_1.round)(gross * (1 - (l.discPct ?? 0) / 100));
        subtotal += gross;
        if (l.kind === 'jasa')
            netService += net;
        else
            netGoods += net;
        return net;
    });
    const net = netGoods + netService;
    const ppn = (0, money_js_1.round)(net * rate);
    return { lines: nets, subtotal, discount: subtotal - net, net, netGoods, netService, ppn, total: net + ppn };
}
function lineProblems(l, i) {
    const out = [];
    const n = `Baris ${i + 1}`;
    if (!(l.qty > 0))
        out.push(`${n}: kuantitas harus lebih dari nol.`);
    if (!Number.isInteger(l.price) || l.price < 0)
        out.push(`${n}: harga harus rupiah bulat ≥ 0.`);
    if ((l.discPct ?? 0) < 0 || (l.discPct ?? 0) > 100)
        out.push(`${n}: diskon 0–100%.`);
    return out;
}
const addDays = (iso, days) => {
    const d = new Date(iso + 'T00:00:00Z');
    d.setUTCDate(d.getUTCDate() + days);
    return d.toISOString().slice(0, 10);
};
exports.addDays = addDays;
const daysBetween = (a, b) => Math.round((Date.parse(b + 'T00:00:00Z') - Date.parse(a + 'T00:00:00Z')) / 86_400_000);
exports.daysBetween = daysBetween;
/** Status tersimpan dari nilai & pembayaran. Jatuh tempo dihitung saat tampil, bukan disimpan. */
function invoiceStatus(total, paid) {
    if (paid <= 0)
        return 'belum-dibayar';
    return paid >= total ? 'lunas' : 'sebagian';
}
function overdueDays(dueDate, asOf, open) {
    return open > 0 ? Math.max(0, (0, exports.daysBetween)(dueDate, asOf)) : 0;
}
exports.AGING_BUCKETS = [
    { key: 'current', label: 'Belum jatuh tempo', min: -Infinity, max: 0 },
    { key: 'd30', label: '1–30 hari', min: 1, max: 30 },
    { key: 'd60', label: '31–60 hari', min: 31, max: 60 },
    { key: 'd90', label: '61–90 hari', min: 61, max: 90 },
    { key: 'over90', label: 'Lebih dari 90 hari', min: 91, max: Infinity },
];
const agingKey = (dueDate, asOf) => {
    const d = (0, exports.daysBetween)(dueDate, asOf);
    return exports.AGING_BUCKETS.find((b) => d >= b.min && d <= b.max).key;
};
exports.agingKey = agingKey;
function aging(docs, asOf) {
    const buckets = exports.AGING_BUCKETS.map((b) => ({ key: b.key, label: b.label, value: 0, count: 0 }));
    for (const d of docs) {
        if (d.open <= 0)
            continue;
        const b = buckets.find((x) => x.key === (0, exports.agingKey)(d.dueDate, asOf));
        b.value += d.open;
        b.count += 1;
    }
    return buckets;
}
/**
 * Pesanan butuh persetujuan manajer bila pelanggan ditahan, nilai melebihi sisa
 * plafon (bila kebijakan memblokir), atau melampaui batas persetujuan perusahaan.
 */
function creditCheck(customer, exposure, amount, policy) {
    const reasons = [];
    const available = customer.creditLimit - exposure;
    if (customer.status === 'ditahan')
        reasons.push('Pelanggan berstatus ditahan.');
    if (amount > available && policy.blockOverCreditLimit)
        reasons.push(`Nilai pesanan melebihi sisa plafon kredit (sisa Rp ${Math.max(0, available).toLocaleString('id-ID')}).`);
    if (policy.salesApprovalThreshold > 0 && amount > policy.salesApprovalThreshold)
        reasons.push(`Nilai pesanan di atas batas persetujuan Rp ${policy.salesApprovalThreshold.toLocaleString('id-ID')}.`);
    return { needsApproval: reasons.length > 0, reasons, available, exposureAfter: exposure + amount };
}
/** Termin → jumlah hari ("Net 30" → 30, "Tunai" → 0). */
const termDays = (terms) => (typeof terms === 'number' ? terms : Number(/(\d+)/.exec(terms)?.[1] ?? 0));
exports.termDays = termDays;
