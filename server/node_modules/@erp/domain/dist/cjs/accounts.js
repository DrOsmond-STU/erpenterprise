"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AccountIndex = exports.categoryOf = exports.normalSideOf = exports.isDebitNormal = exports.SALARY_PAYABLE = exports.ACCUM_DEPR = exports.FIXED_ASSET_ACCOUNTS = exports.INVENTORY_ACCOUNTS = exports.CURRENT_EARNINGS = exports.AP_ACCOUNT = exports.AR_ACCOUNT = exports.CASH_ACCOUNT = exports.RK_PUSAT = exports.RK_CABANG = void 0;
exports.fromPrototype = fromPrototype;
exports.RK_CABANG = '1-3100'; // buku kantor pusat
exports.RK_PUSAT = '3-1500'; // buku cabang
exports.CASH_ACCOUNT = '1-1100';
exports.AR_ACCOUNT = '1-1200';
exports.AP_ACCOUNT = '2-1100';
exports.CURRENT_EARNINGS = '3-3000';
exports.INVENTORY_ACCOUNTS = ['1-1400', '1-1450', '1-1500'];
exports.FIXED_ASSET_ACCOUNTS = ['1-2300', '1-2400', '1-2500'];
exports.ACCUM_DEPR = '1-2900';
exports.SALARY_PAYABLE = '2-1200';
const isDebitNormal = (code) => /^[15]/.test(code);
exports.isDebitNormal = isDebitNormal;
const normalSideOf = (code) => ((0, exports.isDebitNormal)(code) ? 'debit' : 'credit');
exports.normalSideOf = normalSideOf;
const categoryOf = (code) => ({ '1': 'Aset', '2': 'Liabilitas', '3': 'Ekuitas', '4': 'Pendapatan', '5': 'Beban' }[code[0]]);
exports.categoryOf = categoryOf;
/** Mengubah baris bagan akun purwarupa (`DATA.chartOfAccounts`) menjadi `Account`. */
function fromPrototype(raw) {
    return {
        code: raw.code,
        name: raw.name,
        type: raw.type === 'Header' ? 'header' : 'detail',
        category: raw.category,
        parentCode: raw.parent,
        level: raw.level,
        normalSide: (0, exports.normalSideOf)(raw.code),
        isIntercompany: Boolean(raw.interco),
        isContra: Boolean(raw.contra),
        isCash: raw.code === exports.CASH_ACCOUNT,
        isComputed: Boolean(raw.computed),
        status: raw.status,
    };
}
class AccountIndex {
    byCode = new Map();
    list;
    constructor(accounts) {
        this.list = [...accounts].sort((a, b) => a.code.localeCompare(b.code));
        for (const a of this.list)
            this.byCode.set(a.code, a);
    }
    get(code) { return this.byCode.get(code); }
    name(code) { return this.byCode.get(code)?.name ?? code; }
    details() { return this.list.filter((a) => a.type === 'detail'); }
    children(parentCode) { return this.list.filter((a) => a.parentCode === parentCode); }
    isDetail(code) { return this.byCode.get(code)?.type === 'detail'; }
    isIntercompany(code) { return Boolean(this.byCode.get(code)?.isIntercompany); }
}
exports.AccountIndex = AccountIndex;
