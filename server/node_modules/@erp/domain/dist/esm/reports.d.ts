import type { AccountIndex } from './accounts.js';
import type { Account, BalanceMap, BalanceSheet, Consolidation, IncomeStatement, ISODate, PostedLine, Rupiah, TrialBalance } from './types.js';
/** Menyusun peta saldo dari baris terposting untuk rentang periode. Nilai bertanda sisi normal. */
export declare function balancesFromLines(lines: PostedLine[], from: ISODate, to: ISODate): BalanceMap;
/** Menyusun peta saldo dari agregat SQL: baris {code, openingDebit, openingCredit, debit, credit}. */
export declare function balancesFromAggregates(rows: {
    code: string;
    openingDebit: number;
    openingCredit: number;
    debit: number;
    credit: number;
}[]): BalanceMap;
export declare function sumBalances(maps: BalanceMap[]): BalanceMap;
export declare function trialBalance(accounts: AccountIndex, bal: BalanceMap): TrialBalance;
export interface PlGroupDef {
    id: string;
    label: string;
    match: (a: Account) => boolean;
    sign: 1 | -1;
}
/** Pengelompokan laba rugi diturunkan dari struktur bagan akun (kode & induk). */
export declare const PL_GROUPS: PlGroupDef[];
/** Nilai periode per akun laba rugi; positif = menambah laba. */
export declare function plAmounts(accounts: AccountIndex, bal: BalanceMap): Record<string, Rupiah>;
export declare function incomeStatement(accounts: AccountIndex, bal: BalanceMap): IncomeStatement;
/** Laba (rugi) sejak awal tahun buku: Σ pendapatan − Σ beban dari saldo s.d. akhir periode (opening + mutasi). */
export declare function ytdProfit(accounts: AccountIndex, bal: BalanceMap): Rupiah;
export declare function balanceSheet(accounts: AccountIndex, bal: BalanceMap, asOf: ISODate): BalanceSheet;
export declare function consolidate<T>(accounts: AccountIndex, perBranch: {
    branch: string;
    label: string;
    balances: BalanceMap;
}[], build: (bal: BalanceMap) => T): Consolidation<T>;
/** Selisih eliminasi: RK Cabang (pusat) harus sama dengan Σ RK Kantor Pusat (cabang). */
export declare function intercompanyMismatch(accounts: AccountIndex, combined: BalanceMap): Rupiah;
