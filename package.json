{
  "name": "erpenterprise",
  "version": "0.2.0",
  "private": true,
  "description": "ERP Enterprise — purwarupa UI/UX dan implementasi produksi (Vue 3 + NestJS + PostgreSQL)",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "node tools/build.mjs",
    "test": "node tools/build.mjs && node tools/smoke.mjs",
    "domain:test": "npm run test -w @erp/domain",
    "build:all": "npm run build -w @erp/domain && npm run build -w @erp/api && npm run build -w @erp/web",
    "api:dev": "npm run dev -w @erp/api",
    "api:migrate": "npm run migrate -w @erp/api",
    "api:seed": "npm run seed -w @erp/api",
    "web:dev": "npm run dev -w @erp/web",
    "check": "npm run typecheck -w @erp/domain && npm run typecheck -w @erp/api && npm run typecheck -w @erp/web && npm run domain:test",
    "api:e2e": "npm run test:e2e -w @erp/api",
    "web:e2e": "node tools/web-e2e.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DrOsmond-STU/erpenterprise.git"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "playwright": "^1.49.1",
    "typescript": "~5.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
